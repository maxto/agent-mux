#!/usr/bin/env bash
# Mock tmux for install tests (not used in tmux-agent functional tests)
case "$1" in
  -V)            echo "tmux 3.3a" ;;
  list-sessions) exit 0 ;;
  source-file)   exit 0 ;;
  *)             exit 0 ;;
esac
